import { Theme, GlobalStyles} from 'themes';
import { Routes } from 'routes';

const App = () => {
  return (
    <Theme>
      <GlobalStyles />
      <Routes />
    </Theme>
  );
};

export default App;
